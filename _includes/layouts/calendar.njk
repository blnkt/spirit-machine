---
layout: layouts/base.njk
section: calendar
---
<div class="content-wrap">
  <h1>{{ title }}</h1>
  {{ content | safe }}

  <section class="events-upcoming" aria-labelledby="events-upcoming-heading">
    <h2 id="events-upcoming-heading">Upcoming</h2>
    {% if collections.upcomingEvents | length %}
      <div class="event-list">
        {% for item in collections.upcomingEvents %}
          {% set past = false %}
          {% include "components/event-card.njk" %}
        {% endfor %}
      </div>
    {% else %}
      <p>No upcoming events scheduled. Check back soon.</p>
    {% endif %}
  </section>

  <section class="events-past" aria-labelledby="events-past-heading">
    <h2 id="events-past-heading">Past</h2>
    {% if collections.pastEvents | length %}
      <div class="event-list">
        {% for item in collections.pastEvents %}
          {% set past = true %}
          {% include "components/event-card.njk" %}
        {% endfor %}
      </div>
    {% else %}
      <p>No past events listed yet.</p>
    {% endif %}
  </section>
</div>
